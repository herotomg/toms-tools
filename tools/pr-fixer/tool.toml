id = "pr-fixer"
name = "PR Fixer (Claude Code)"
description = "Install the pr-fixer Claude Code agent and /fix-pr slash command."
version = "1"
depends = ["gh-unresolved"]
status_check = "test -f \"$HOME/.claude/agents/pr-fixer.md\" && test -f \"$HOME/.claude/commands/fix-pr.md\""
