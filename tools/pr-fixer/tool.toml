id = "pr-fixer"
name = "PR Fixer"
description = "A /fix-pr slash command that fixes unresolved review comments on the current PR."
version = "5"
depends = ["gh-unresolved"]
next_steps = "In Claude Code, run `/fix-pr` on a PR branch."

installs = ["~/.claude/commands/fix-pr.md"]
