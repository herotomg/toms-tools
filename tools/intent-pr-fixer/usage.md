# Intent PR Fixer

1. Install `gh-unresolved` first (this tool depends on it).
2. Open Intent and create an agent.
3. Pick the `PR Fixer` specialist.
4. On a PR branch, ask it to fix unresolved CR comments.